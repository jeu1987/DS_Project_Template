# This is a folder structure format for a data science project
By default, the contents of the folders are not added to git. You can change this by updating the .gitignore file in the respective folders. 

